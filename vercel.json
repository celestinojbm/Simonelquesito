{
  "buildCommand": "if [ \"$SEED_ONCE\" = \"1\" ]; then BUSINESS_PRESET=salsamentaria npm run db:seed; fi && next build",
  "crons": [
    {
      "path": "/api/cron/canastas",
      "schedule": "0 11 * * *"
    }
  ]
}
